extends layout

block content
  div(class="error-image")
    div(class="text-center font-weight-bolder")
      div(class="col-md-12 p-lg-5 mx-auto my-5 text-danger")
        h1= message
        h2= error.status
        pre(class="text-dark") #{error.stack}
